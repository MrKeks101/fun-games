[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=games --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
branch = true
source = ["games"]

[tool.coverage.report]
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
]
