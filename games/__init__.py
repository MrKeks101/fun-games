"""Game packages for the ``fun-games`` collection.

Each subpackage is a standalone Python + ``pygame`` desktop game. Game-rule and
AI logic live in plain modules with no ``pygame`` import so they stay
unit-testable without a display.
"""
